import threading
import logging

# ===========================================================================
# Scheduler
#
# the scheduler is a daemon that runs in the background as a thread and runs
# actions at a certain time. It provides functionality for adding actions,
# removing actions and getting the current status (i.e. what action is running)
# ===========================================================================

class Scheduler(threading.Thread):
    def __init__():

    def start():

    def stop():
    """stop the scheduler"""

    def clear():
    """remove all scheduled tasks"""